# inAct Journal — Nginx production config (generic reference)
# The deploy script uses nginx/nginx-production.conf; this file is a plain sample.
# Place this in /etc/nginx/sites-available/inact and symlink to sites-enabled/

upstream django {
    server 127.0.0.1:5002;
}

server {
    listen 80;
    server_name inact.lmta.lt www.inact.lmta.lt;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name inact.lmta.lt www.inact.lmta.lt;

    # SSL — managed by Certbot
    ssl_certificate     /etc/letsencrypt/live/inact.lmta.lt/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/inact.lmta.lt/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # Static files (served directly by Nginx)
    location /static/ {
        alias /var/www/inact/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Media files (uploaded content — local storage)
    location /media/ {
        alias /var/www/inact/media/;
        expires 7d;
        # Restrict direct access to private assets if needed
    }

    # Django application
    location / {
        proxy_pass http://django;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        client_max_body_size 500M;  # allow large media uploads
    }

    # Security headers
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header Referrer-Policy "same-origin";
}
